import React from 'react'
import ReactDOM from 'react-dom/client'
import RushUnified from '../rush-full.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RushUnified />
  </React.StrictMode>,
)
